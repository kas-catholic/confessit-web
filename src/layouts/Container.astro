---
import Layout from '@layouts/Layout.astro'
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Layout title={title}>
  <div class="container mx-auto">
    <slot />
  </div>
</Layout>
